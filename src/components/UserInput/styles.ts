import styled from '@emotion/styled'

const SUserInput = styled.div`
  .page_type {
    margin-top: 1em;
    margin-bottom: 0.5em;
  }

  input {
  }
  .chakra-button {
    :focus {
      box-shadow: none;
    }
  }
`

export { SUserInput }
